apiVersion: v1
kind: Secret
metadata:
  name: udb-secrets
  namespace: udb
type: Opaque
stringData:
  db-password: "udb"
  database-url: "postgresql://udb:udb@postgres:5432/udb?schema=public"
  jwt-secret: "replace-with-production-secret"
  stripe-secret-key: ""
  stripe-webhook-secret: ""
