# Interventions & Nudges (Lean Phase 2)

- Nudges: reminder to use planner; prompts to use hints after a missed task
- Simplify: if user shows confusion, simplify UI to core features only
- Disable: if the cost guard flags a spike, disable AI hints temporarily
- Disable: if vector fallback too costly/unreliable, suppress vector use
